# Changelog content

Drop `.md`/`.mdx` files in this directory to publish changelog entries. Schema is defined in `src/content.config.ts` (the `changelog` collection).

Required frontmatter: `version`, `date`, `summary`.

No changelog listing page exists yet (v1 ships the homepage only) — this is scaffolded so the collection can be wired up without restructuring later.
